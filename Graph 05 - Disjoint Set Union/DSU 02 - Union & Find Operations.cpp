1> Each set contains its representative / leader.

2> Union() method first checks that x and y have different representatives using find() (if yes then there is cycle)  then
combines two sets operation.