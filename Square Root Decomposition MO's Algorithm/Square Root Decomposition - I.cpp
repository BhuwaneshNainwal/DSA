1> Query optimization technique.
2> Reduces time to sqrt(N).
3> Divide the array into size of sqrt(N).
4> Compute some answers for every block [L , R].
5> Query L to R and combine answwers for the blocks [L , R].